# Install everything (core + GraphRAG).
#
# For a lighter install (recommended for customers evaluating the core KG),
# use `requirements-core.txt`.
-r requirements-core.txt
-r requirements-graphrag.txt

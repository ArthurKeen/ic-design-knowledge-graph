[pytest]
testpaths = tests
pythonpath = src
addopts = -v

markers =
    integration: mark test as requiring a live ArangoDB / GenAI API connection (skipped by default)
    slow: mark test as slow / expensive (e.g. full import workflow); run manually only
